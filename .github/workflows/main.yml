name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x, 22.x]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - run: npm ci
      - run: npm run build

      # Browser-free unit tests for the introspection stack (J-space math,
      # hazard detection, calibration, train of thought) — no Chromium needed.
      - name: Introspection unit tests
        run: npm run test:introspection

      - name: Use Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Validate Python package
        run: |
          python -m pip install --upgrade pip
          pip install hatchling mcp httpx pydantic
          python -m compileall python/splice_mcp
